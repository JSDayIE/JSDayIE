import * as React from "react";
import { Section, Container } from "../../../lib/components";

export function Accessibility() {
    return (
        <Container>
            <Section title="Accessibility" size={1}>
                TODO
            </Section>
        </Container>
    );
}
